# variant, use suite name as default value
ruyi:
  cases:
    # testcases list
    - ruyi-basic
  pre:
    # each pre script should have a corresponding post script
    # or set it to _
    - ["ruyi-pre", ]
  post:
    - ["ruyi-post", ]

